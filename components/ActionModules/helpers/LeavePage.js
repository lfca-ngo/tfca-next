import { Button } from 'antd'
import React from 'react'

export const LeavePage = ({ onNext }) => {
  return (
    <div>
      LeavePage
      <Button onClick={onNext}>Count me in</Button>
    </div>
  )
}
